import 'package:flutter/material.dart';

class WordleBorders {
  static final BorderRadius radiusS = BorderRadius.circular(2.5);
  static final BorderRadius radiusM = BorderRadius.circular(5);
  static final BorderRadius radiusL = BorderRadius.circular(10);
  static const BorderRadius radiusNone = BorderRadius.zero;

  static const BoxShape radiusRound = BoxShape.circle;
}
